app: Artisan

ingredients:
  dist: trusty
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ trusty main universe
  script:
    - path="file:///$(pwd)/$(ls artisan-*.deb)"
    - curl -O -C - $path
script:
