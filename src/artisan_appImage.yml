app: Artisan

ingredients:
  dist: trusty
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ trusty main universe
  script:

    - path="file:///$(cd ..; pwd)/$(cd .. ; ls artisan-*.deb)"
    - curl -O -C - $path
script:
