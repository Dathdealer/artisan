app: Artisan

ingredients:
  dist: trusty
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ trusty main universe
  script:
    - folder="$(cd ..; pwd)"
    - echo  $folder
    - echo "$(cd $folder; ls)
    - file="$(cd $folder; ls artisan-*.deb)"
    - echo  $file
    - path="file:///$folder/$file"
    - curl -O -C - $path
script:
