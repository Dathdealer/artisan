app: Artisan

ingredients:
  dist: trusty
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ trusty main universe
  script:
    - file=$(ls artisan-*.deb)
    - path="file:///$(pwd)/artisan-*.deb"
    - curl -O -C - $path
script:
